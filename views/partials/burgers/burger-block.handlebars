          <li>{{this.id}} {{this.burger_name}}
            <button class="btn btn-primary float-right change-devour" data-id="{{id}}" data-newdevour="{{devoured}}">
              Devour It!
            </button>
          </li>